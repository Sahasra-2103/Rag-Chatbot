// RAGAS runner placeholder
// Implemented next step after dependencies are added.

export async function runRagasEvaluation({ query, answer, retrievedContexts = [], groundTruth = '', llm = null }) {
  // Fallback: return null to keep app running until RAGAS deps are wired.
  return null;
}

